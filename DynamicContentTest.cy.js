import Scrolling from "../../support/Page Object/DynamicContentPage.cy";

describe('', () => {

    const scroll=new Scrolling();
    it('', () => {
        scroll.enterURL();
        scroll.scrollTo();
    });
});